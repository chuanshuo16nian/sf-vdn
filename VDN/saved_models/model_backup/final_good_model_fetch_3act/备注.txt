模型使用的100000大小的经验池，每次取32个大小的minibatch，训练了10000000个step。gamma=0.99，learning_rate=0.00025,epsilon从1递减到0.每10000个step更新一次target网络
网络结构：对每个agent：
	  输入层：25 * 4维向量（有连续4帧的灰度图展开组成）
	  第一个隐藏层：128
	  第二个隐藏层：64
	  输出层：3（3个动作。0代表前进，1代表左转，2代表不动）
	  最后输出的Q值向量组成9维的联合Q值。